#include<iostream>
#include<cstdio>
using namespace std;

int main()
{

    std::ios::sync_with_stdio(false);
    short int ch;
    cin>>ch;
    if(ch==29||ch==30)
        cout<<"YES\n";
    else
        cout<<"NO\n";
  return 0;
}
